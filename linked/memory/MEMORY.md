# Global Memory

Single shared memory / global instructions for every AI tool (Claude, Codex,
and future models). This file is the source of truth; each tool links to it
under its own expected filename:

- Claude Code -> `~/.claude/CLAUDE.md`
- OpenAI Codex -> `~/.codex/AGENTS.md`

Edit this file once and every linked tool picks up the change.

<!-- Add durable, cross-tool instructions and facts below. -->
